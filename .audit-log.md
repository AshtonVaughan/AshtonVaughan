# Profile README audit loop

Iterations completed: 7 / 10

## Log

### Iteration 1 / 10 - 2026-04-26
**Critique:** Banner had a fake terminal box on the right showing PROJECTS/LANGUAGES/LATEST/STATUS rows. Filler text dressed up as a UI window. Same AI-slop pattern that was removed from the about card last session, just relocated to the banner.
**Fix:** Deleted the entire terminal `<g>` block from banner.svg. Banner now shows only the name, the dollar-prompt tagline, and the IT/QUT/hacking-since-9 subtitle. No fake UI chrome.
**Next target:** The "SYSTEM ONLINE / SESSION ACTIVE" line at the top-left of the banner is still filler. Same family of slop. Likely cut next.

### Iteration 2 / 10 - 2026-04-26
**Critique:** "SYSTEM ONLINE / SESSION ACTIVE" line with a pulsing green dot at the top-left of the banner. Pure fake-hacker-UI cliche. Says nothing real, just signals "this is meant to look technical."
**Fix:** Deleted the `<g>` containing the green pulse circle and the SYSTEM ONLINE text. Banner top is now empty above the name, which gives the typography more room to breathe.
**Next target:** The four corner HUD brackets (decorative geometry). They are framing nothing and read as "hacker movie poster" filler. The right half of the banner is also visually empty now since the terminal is gone, banner may benefit from a height reduction next.

### Iteration 3 / 10 - 2026-04-26
**Critique:** Four pink L-shaped corner brackets in the banner framing nothing. Pure decorative geometry that violates the "every visual element earns its place by carrying information" rule. Reads as hacker-movie-poster cliche.
**Fix:** Removed the entire `<g>` containing the 4 corner bracket `<path>` elements. Banner now has just background + accent lines + name + tagline + subtitle.
**Next target:** Reduce banner viewBox height from 280 to ~180. After cutting the terminal, SYSTEM ONLINE line, and corner brackets, the bottom ~120px of the banner is dead space. Tighten the canvas around the actual content.

### Iteration 4 / 10 - 2026-04-26
**Critique:** Banner viewBox was 1200x280 but the actual content (name + tagline + subtitle) only occupied 80px of vertical space. The banner had ~150px of dead grid pattern with no content, making it feel like a half-finished poster.
**Fix:** Compressed banner viewBox from 1200x280 to 1200x180. Repositioned name at y=80, tagline at y=120, subtitle at y=146, bottom accent at y=178. Content now occupies the full canvas with appropriate margins.
**Next target:** The empty horizontal void between the badges row and the about section. After the badges, there's a horizontal rule and "./about" header but the visual weight is uneven. Also the typing-svg in the README header may be redundant with the banner subtitle. Will check next iteration.

### Iteration 5 / 10 - 2026-04-26
**Critique:** Typing-svg below the banner cycled five lines, but lines 1-2 ("18yo IT student / QUT / Brisbane", "Hunting exploits on HackerOne since I was 9") are verbatim what the banner subtitle already says, and lines 3-5 repeat the project taglines that appear three sections down in `./shipping`. Pure redundancy plus animation for animation's sake.
**Fix:** Removed the entire `<a><img>` block referencing readme-typing-svg from README.md. The banner now stands alone as the page's typographic anchor.
**Next target:** The badges row alignment looks visually weak. Each badge has different perceived weight due to logo styles and the implied prefix glyphs (envelope, h1, snake, in). Also the `./signal` section has duplicate stats card and streak that may overlap or feel redundant. Likely the badges first.

### Iteration 6 / 10 - 2026-04-26
**Critique:** The ethos line at the bottom of `./about` was wrapped in a triple-backtick code block, which made GitHub render it as a code snippet with a "copy code" icon button. Nothing useful to copy. Wrong formatting treatment: applied a code-block UX affordance to plain prose.
**Fix:** Replaced the fenced code block with four inline code spans separated by `&nbsp;·&nbsp;`, one per domain. Each domain now reads as a subtle tag without the block-level copy button.
**Next target:** the dollar-prompt prefix (`$`) on the banner tagline `$ offensive_security + ai_ml_systems`. Implies it's a shell command but it's not. Mildly affected. Will assess whether to drop the `$` or restructure.

### Iteration 7 / 10 - 2026-04-26
**Critique:** Banner tagline started with `$` shell-prompt prefix, implying the line was a command being typed in a terminal. It is not a command, just a label. Same family of fake-terminal slop already removed from elsewhere.
**Fix:** Removed the `$ ` prefix from the banner tagline. The remaining `offensive_security + ai_ml_systems` reads cleanly as a snake-case domain label, which is legitimate stylistic choice without the fake-shell pretension.
**Next target:** The bio under the avatar (set in GitHub profile settings, not README) still says "University student at QUT with a decade of side projects. Building AI-powered security tools. Bug bounty hunter on HackerOne." This contradicts the README's specific framing ("IT student", "hunting exploits", "since I was 9"). Update via gh API.
